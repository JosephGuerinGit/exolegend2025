#include "utils/strategy.h"
#include "utils/planner.h"

bool is_moving = false;

void reset_state_machine(){
    // reset state machine
    is_moving = false;
}

bool isMoving() {
    return true;
}

void run() {
    while (true) {

    }
}